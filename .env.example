# Credentials for the live test suite (tests/live/).
# Copy this file to `.env` and adjust the values for your broker.
#
#   cp .env.example .env
#
# These match a default `rabbitmq:4-management` container.
RABBIT_HOST=localhost
RABBIT_PORT=15672
RABBIT_USER=guest
RABBIT_PASS=guest
