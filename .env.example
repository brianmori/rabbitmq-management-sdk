# Credentials for the live test suite (tests/live/).
# Copy this file to `.env` and adjust the values for your broker.
#
#   cp .env.example .env
#
# These match the disposable Docker Compose brokers used by the Just recipes.
RABBIT_HOST=localhost
RABBIT_PORT=15672
RABBIT_USER=sdk_test
RABBIT_PASS=sdk_test
